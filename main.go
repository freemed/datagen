package main

import (
	"fmt"
)

func main() {
	fmt.Println("# https://github.com/freemed/datagen")

	/*
		ng := NewNameGenerator()
		for i := 0; i < 10; i++ {
			f, l := ng.Generate(Male)
			fmt.Println(f + " " + l)
		}
	*/
}
